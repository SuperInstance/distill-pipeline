[package]
name = "distill-pipeline"
version = "0.1.0"
edition = "2021"
description = "Progressive distillation pipeline for the Grand Pattern — rooms learn, distill, and re-emerge smarter"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
